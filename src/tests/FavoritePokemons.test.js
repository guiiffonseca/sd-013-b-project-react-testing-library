import React from 'react';

describe('testa o componente Favorite Pokemons', () => {
  test('teste se é exibido No favorite pokemon na tela', () => {});

  test('teste se é exibido todos os cards de pokemons favoritados', () = > {
    
  })

})
